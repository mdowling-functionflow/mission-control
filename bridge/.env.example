# Local bridge authentication token (must match BRIDGE_TOKEN in cloud backend)
BRIDGE_TOKEN=change-me-to-a-secure-random-token

# Path to OpenClaw installation
OPENCLAW_DIR=/Users/michaeldowling/.openclaw
